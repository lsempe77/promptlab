# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.mypy_cache/

# Env / secrets (defense in depth; *.local already covers .env.local)
.env
.env.*
!.env.example

# Databases & large local data
*.db
*.sqlite
*.sqlite3

# Generated analysis / inspection artifacts
analysis.html
analysis_raw.txt
analysis_data.txt
*.png
wrong_labels_report.txt
gt_fixes_*.csv
gt_fixes_backup_*.json
test_fields.csv
# Model-vs-GT disagreement exports (contain author names = PII; local only)
analysis_disagreements/

# Local working material merged in from the old DEP workspace (2026-08-21).
# Kept on disk, deliberately out of git: large binaries and source data that
# would bloat the repo and the Docker build context.
*.xlsx
*.docx
*.pdf
*.zip
archive/
backups/
# Curator hand-off packs: contain full paper text + author names (PII).
double-extraction/
backend/data/curator2_*

# OneDrive folder-metadata junk
desktop.ini
**/desktop.ini
# Quarto render output — regenerable from the .md/.qmd sources beside it
docs/*_files/
docs/*.html
